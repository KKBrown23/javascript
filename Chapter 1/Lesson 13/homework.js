// 1. Write a program that will take 5 numbers save them into an array,
// then it will print all the negative numbers.
// In - Take in 5 numbers
// Cal - save them into an array and pick out the negative numbers
// Out - print out the negative numbers

// let userNumberList = [];
// for (let counter = 0; counter < 5; counter += 1) {
//   let numbers = +prompt("Enter a number");
//   userNumberList.push(numbers);
// }

// let index = 0;
// for (let counter = 0; counter < 5; counter += 1) {
//   let negativeNumber = userNumberList[index];
//   if (negativeNumber < 0) {
//     console.log(negativeNumber);
//   }
//   index += 1;
// }

// 2. Write a program that will take 5 names save them into an array,
// then print them from the last name to the first one
// EX: KK , BB, TT, YY, UU => UU, YY, TT, BB, KK
// In - 5 names
// Cal - rearrange them
// Out - return backwards

// let namesList = ["George", "Max", "Lewis", "Fred", "Liam"];

// let index = namesList.length - 1;
// for (let counter = 0; counter < 5; counter += 1) {
//   let name = namesList[index];
//   console.log(name);
//   index -= 1;
// }
