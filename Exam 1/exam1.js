// write a program to read 5 names from the user and save them into an array
// In - taking in 5 names and saving them in an array
// Cal -
// Out - the 5 names

// let fiveNamesList = [];
// for (let counter = 0; counter < 5; counter += 1) {
//   let name = prompt("Please enter a name");
//   fiveNamesList.push(name);
// }

// console.log(fiveNamesList);

// write a program that declares and array of numbers,
//then it reads all the numbers and prints out only the positive ones
// In - declare an array of numbers
// Cal - read all the numbers and calculate which ones are postive using if statements
// Out - printing out the positive ones

// let postiveNumbers = [];
// postiveNumbers = [5, -1, 2, 10, 4];

// let index = 0;
// for (let counter = 0; counter < 5; counter += 1) {
//   let number = postiveNumbers[index];
//   index += 1;

//   if (number > 0) {
//     console.log(number);
//   }
// }

//RESULT: A (9/10)
