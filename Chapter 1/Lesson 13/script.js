//Take 5 numbers from the user and save them into an array
// Read all the elements of the array and print  only the ones that are positive

// In - 5 numbers from the user using a loop and an array
// Cal - Read all the elements using a loop and an index
//      For each element check if it`s positvive
// Out - if the element is psitive print it out

// let userNumbersList = [];
// for (let counter = 0; counter < 5; counter += 1) {
//   let userNumber = prompt("Enter a number");
//   userNumbersList.push(userNumber);
// }

// let index = 0;
// for (let counter = 0; counter < 5; counter += 1) {
//   let listElement = userNumbersList[index];
//   if (listElement >= 0) {
//     console.log(listElement);
//   }
//   index += 1;
// }
