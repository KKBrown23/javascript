// 1. write a function that takes as parameter 2 numbers and returns the smallest of them

// In - take two numbers as a parameter
// Cal - compare the two numbers, save it in a variable
// Out - returns the smallest out of the two numbers

// function getMinimal(number1, number2) {
//   if (number1 > number2) {
//     return number2;
//   } else {
//     return number1;
//   }
// }

// let minimum = getMinimal(2, 8);
// console.log(minimum);

// 2. Write a function that generates and returns a random number
// In = Nothing
// Cal = generating a number using ".random"
// Out =  return the random number

// function generateRandom() {
//   let generatedNumber = Math.random() * 10;
//   generatedNumber = Math.floor(generatedNumber);
//   return generatedNumber;
// }

// let randomNumber = generateRandom();
// console.log(randomNumber);

// 3. Write a function that takes a name and returns "Hello" + the name
// In - a name from the user
// Cal - create a string that merges hello and the name
// Out - return Hello + the name

// function returnHello(username) {
//   let greeting;
//   greeting = "Hello" + username;
//   return greeting;
// }

// let greeting = returnHello("KK");
// console.log(greeting);

// 4. Write a function that takes 2 numbers and return true if
//      their product is the same as their sum, else returns false

// In - taking two numbers as a parameter
// Cal - calculate the sum and the product then compare the sum and the product
// Out - if the product is the same as the sum return true otherwise return false

// function compareSumAndProduct(number1, number2) {
//   let sum;
//   sum = number1 + number2;

//   let product;
//   product = number1 * number2;

//   if (sum == product) {
//     return true;
//   } else {
//     return false;
//   }
// }

// let result = compareSumAndProduct(2, 2);
// console.log(result);

// compareSumAndProduct(2, 2)

//https://www.youtube.com/watch?v=FOD408a0EzU
